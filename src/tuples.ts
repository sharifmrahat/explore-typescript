let b: [string, number, object] = ["test", 45, { t: 1 }];

// b[1] = 'test' //Index number 1 is a number so we can not change it as string.

//Tuples actually saves the reference on an array and its inner value type.
